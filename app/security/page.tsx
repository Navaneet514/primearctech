import type { Metadata } from "next";
import { TrustPage } from "../TrustPage";

export const metadata: Metadata = { title: "Security and safeguards", description: "Plain-language FieldRelay architecture, data handling, call safeguards, and product limitations.", alternates: { canonical: "/security" } };

export default function SecurityPage() { return <TrustPage eyebrow="Security and safeguards" title="Control before automation." intro="FieldRelay is designed as a constrained intake layer. The demo cannot make a real booking, dispatch, transfer, estimate, or CRM write."><section><h2>Architecture</h2><p>Browser audio goes to the configured voice provider, then through an immutable FieldRelay policy. Authenticated webhooks can attach the call ID, transcript, policy decision, and provisional dispatcher receipt to a short-lived D1 demo session.</p><a className="trust-download" href="/fieldrelay-architecture-brief.pdf">Download one-page architecture brief</a></section><section><h2>Immutable boundaries</h2><ul><li>No diagnosis or repair instruction.</li><li>No firm estimate, service promise, or confirmed appointment.</li><li>Danger language stops routine intake.</li><li>The public demo cannot perform a real human handoff.</li><li>Prompt-injection attempts do not replace the system policy.</li></ul></section><section><h2>Data and retention</h2><p>Public demo sessions expire after two hours; pitch sessions after 24 hours. Cleanup removes expired sessions after a one-day grace period. Rate limiting stores a salted IP hash, never the raw IP. Application logs must not contain transcripts, names, emails, or prospect profiles.</p></section><section><h2>Audio and recording</h2><p>Microphone access requires an explicit click. Public pitch recording is disabled in the voice assistant configuration. Call participants must still follow applicable notice and consent laws before any production recording is enabled.</p></section><section><h2>Provider and failure limits</h2><p>Voice availability depends on the configured provider and credits. If it fails, the public experience labels its audible scripted sample as an illustrative fallback. Missing webhook storage does not create a false success state.</p></section><section><h2>Production readiness</h2><p>Production launch remains blocked until the previously exposed provider private key is rotated, secrets are configured through hosting, the audit webhook works, legal copy is approved, and founder/company details are supplied.</p></section></TrustPage>; }
